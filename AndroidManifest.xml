<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codefupanda.genie"
    android:versionCode="1"
    android:versionName="1.0-beta" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.codefupanda.genie.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="com.codefupanda.genie.AddActivity"
            android:label="@string/what_can_i_do_for_you" >
        </activity>
        
 		<activity
            android:name="com.codefupanda.genie.WelcomeActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        </activity>

 		<activity
            android:name="com.codefupanda.genie.HelpActivity"
            android:label="@string/action_help">
        </activity>
    </application>

</manifest>
